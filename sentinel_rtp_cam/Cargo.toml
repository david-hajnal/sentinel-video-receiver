[package]
name = "sentinel_rtp_cam"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
tokio = { version = "1", features = [
    "rt-multi-thread",
    "macros",
    "net",
    "io-util",
    "time",
    "process",
    "fs"
] }
base64 = "0.22"
regex = "1"
md5 = "0.7"
rand = "0.8"
url = "2"